APP_MODULES      := libdemo
APP_PLATFORM     := android-10

#APP_OPTIM uses manifest option by default, or can be hard wired to release or default
APP_OPTIM        := release

#Takes longer to build, but then supports all architectures 
APP_ABI          := all

